[package]
name = "notifications"
version = "0.1.0"
edition = "2018"

[dependencies]
actix-web = "4.0"
reqwest = { version = "0.10", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
